# DCM4CHEE PACS Environment Configuration
# ===========================================

# Data Directory Configuration
# Change this to your preferred data storage location
DATA_DIR=./data

# LDAP Configuration
# ===================
LDAP_BASE_DN=dc=dcm4che,dc=org
LDAP_ROOTPASS=secret

# Archive Device Configuration
# ============================
ARCHIVE_DEVICE_NAME=dcm4chee-arc
AE_TITLE=DCM4CHEE

# Network Ports Configuration
# ===========================
DICOM_PORT=11112
HL7_PORT=2575

# Storage Configuration
# ====================
STORAGE_DIR=/storage/fs1

# PostgreSQL Database Configuration
# =================================
POSTGRES_DB=pacsdb
POSTGRES_USER=pacs
POSTGRES_PASSWORD=pacs

# WildFly Configuration
# ====================
WILDFLY_CHOWN=/opt/wildfly/standalone /storage

# Java Memory Configuration
# =========================
# Adjust these values based on your system resources
JAVA_OPTS=-Xms512m -Xmx2g -XX:MetaspaceSize=96M -XX:MaxMetaspaceSize=256m

# Audit Logging / Syslog Configuration
# ====================================
# Configure these values to forward audit records to an external syslog target.
# By default audit logging is effectively disabled because SYSLOG_HOST points to
# the loopback interface inside the container.
SYSLOG_DEVICE_NAME=logstash
SYSLOG_HOST=127.0.0.1
SYSLOG_PORT=514
SYSLOG_PROTOCOL=UDP
SYSLOG_TLS_PORT=6514

# Custom Configuration Examples
# =============================
# Uncomment and modify as needed:

# Custom LDAP Base DN (e.g., for organization)
# LDAP_BASE_DN=dc=hospital,dc=org

# Custom Archive Device Name
# ARCHIVE_DEVICE_NAME=hospital-pacs

# Custom AE Title
# AE_TITLE=HOSPITAL

# Custom Ports (if defaults conflict)
# DICOM_PORT=10104
# HL7_PORT=12575

# Custom Database Credentials
# POSTGRES_DB=hospital_pacs
# POSTGRES_USER=pacs_user
# POSTGRES_PASSWORD=secure_password_here

# Higher Memory Settings for Production
# JAVA_OPTS=-Xms1g -Xmx4g -XX:MetaspaceSize=128M -XX:MaxMetaspaceSize=512m

# Security Configuration
# ======================
# For production, consider:
# - Using strong passwords
# - Enabling HTTPS only
# - Configuring proper SSL certificates
# - Setting up proper firewall rules

# Timezone Configuration
# =====================
# The containers will use the host timezone via volume mounts
# Ensure /etc/timezone exists on your host system

# Backup Configuration
# ===================
# Regular backups of the following directories are recommended:
# - ${DATA_DIR}/db (PostgreSQL data)
# - ${DATA_DIR}/ldap (LDAP data)
# - ${DATA_DIR}/slapd.d (LDAP configuration)
# - ${DATA_DIR}/storage (DICOM images)
# - ${DATA_DIR}/wildfly (WildFly configuration)
